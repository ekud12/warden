// ─── CLI — User-facing commands ──────────────────────────────────────────────
//
// Commands invoked directly by the user (not hook-driven):
//   describe        — show active overrides and rule counts
//   explain         — explain why a specific rule fired
//   export-sessions — JSON/CSV export of session history
//   replay          — replay events through current rules
//   tui             — live terminal dashboard
//   proc            — process management (list, kill stale daemons)
//
// Source: handlers/describe.rs, explain.rs, export_sessions.rs, replay.rs, tui.rs, proc_mgmt.rs
// ──────────────────────────────────────────────────────────────────────────────
